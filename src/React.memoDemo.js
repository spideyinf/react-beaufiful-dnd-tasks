// import React, { Component } from 'react';
// import ReactDOM from 'react-dom'

// const DemoComponent = props => <div>{props.message}</div>

// // Version of React.memo (Replacing React.PureComponent)

// const MemoizedDemoComponent = React.memo(DemoComponent)

// // The Component to trigger rendering of the Demo
// const Counter = props => <strong>{props.count}</strong>

// class MemoDemo extends Component {
//   state = {
//     count: 0,
//     value: 'My message goes here'
//   }

//   handleChange = e => this.setState({ value: e.target.value })
//   handleClick = e => this.setState(({ count }) => ( count: count + 1 ))
// }

// ReactDOM.render(<MemoDemo />, document.getElementById('root'))
